app:
    resource: "../src/Controller/"
    type:     annotation

app_administration:
    resource: "../src/Controller/Administration/"
    type:     annotation
    prefix:   /administration

app_administration_generate:
    resource: "../src/Controller/Administration/Generate"
    type:     annotation
    prefix:   /administration/generate/