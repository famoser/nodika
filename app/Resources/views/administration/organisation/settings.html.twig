{% extends "primary_secondary_content_base.html.twig" %}
{% set color_class = globals_color_class_organisation %}
{% import "macros/buttons.html.twig" as buttons %}
{% import "macros/events.html.twig" as events %}

{% block title %}{{ "settings.title"|trans }}{% endblock %}
{% block description %}{{ "settings.description"|trans }}{% endblock %}

{% trans_default_domain "administration_organisation" %}

{% block primary_content %}
    <h3>{{ "settings.title"|trans }}</h3>
    {{ form(settings_form) }}
{% endblock %}

{% block secondary_content %}
    <h3>{{ "can_confirm_event_before_days"|trans({}, "entity_organisation_setting") }}</h3>
    <p>{{ "settings.can_confirm_event_before_days_description"|trans }}</p>
    <h3>{{ "must_confirm_event_before_days"|trans({}, "entity_organisation_setting") }}</h3>
    <p>{{ "settings.must_confirm_event_before_days_description"|trans }}</p>
    <h3>{{ "send_confirm_event_email_days"|trans({}, "entity_organisation_setting") }}</h3>
    <p>{{ "settings.send_confirm_event_email_days_description"|trans }}</p>
    <h3>{{ "trade_event_days"|trans({}, "entity_organisation_setting") }}</h3>
    <p>{{ "settings.trade_event_days_description"|trans }}</p>
{% endblock %}